# Applets

**Status: not implemented. Planned for M6.**

This file is a placeholder so the design has a home when the work starts. It is
intentionally empty of design detail rather than filled with speculation --
writing the design before the dependency it rests on (see `docs/roadmap.md`)
tends to produce a document that has to be thrown away.

See `docs/roadmap.md` for what M6 covers, the open design questions that must
be settled first, and what it depends on.

Relevant background that already exists:

- `docs/architecture.md` -- the layering this subsystem must respect
- `docs/threat-model.md` -- the threats it will be responsible for
